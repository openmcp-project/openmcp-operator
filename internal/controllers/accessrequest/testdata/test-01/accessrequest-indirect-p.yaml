apiVersion: clusters.openmcp.cloud/v1alpha1
kind: AccessRequest
metadata:
  name: mcr-access-p
  namespace: bar
spec:
  requestRef:
    name: my-cluster-p
    namespace: foo
  permissions:
  - rules:
    - apiGroups:
      - "*"
      resources:
      - "*"
      verbs:
      - "*"
