apiVersion: helm.open-control-plane.io/v1alpha1
kind: HelmDeployerConfig
metadata:
  name: helmdeploy
spec:
  selectorDefinitions:
    single:
      matchPurposes:
      - operator: ContainsAll
        values:
        - single
