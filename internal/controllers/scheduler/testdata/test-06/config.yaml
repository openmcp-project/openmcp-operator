scheduler:
  purposeMappings:
    shared-twice:
      tenancyCount: 2
      template:
        metadata:
          namespace: shared-twice
        spec:
          profile: test-profile
          tenancy: Shared
    shared-thrice:
      tenancyCount: 3
      template:
        metadata:
          namespace: shared-thrice
        spec:
          profile: test-profile
          tenancy: Shared
    shared-four:
      tenancyCount: 4
      template:
        metadata:
          namespace: shared-four
        spec:
          profile: test-profile
          tenancy: Shared
